package model

case class Course(
                     course_id: String,
                     course_name: String,
                     institution: String,
                     course_url: String
                 )
